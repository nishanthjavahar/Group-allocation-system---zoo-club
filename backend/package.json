{
  "name": "zoo-club-group-manager-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend for the Zoo Club Student Group Allocation System",
  "main": "src/server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:watch": "node --test --watch tests/*.test.js"
  },
  "dependencies": {
    "@sparticuz/chromium": "^122.0.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^1.4.5-lts.1",
    "puppeteer-core": "^22.15.0",
    "xlsx": "^0.18.5"
  }
}
